strandsDeps:
  name: strands-deps-${self:provider.stage}
  description: Python dependencies for Strands Agents
  compatibleRuntimes:
    - python3.12
  compatibleArchitectures:
    - aarch64
  package:
    artifact: packaging/dependencies.zip
  retain: false
